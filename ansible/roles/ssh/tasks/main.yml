---
# ============================================================================
# ssh ロール - タスク定義
# ============================================================================
#
# 目的:
#   SSH サーバーの設定を行います
#

- name: OpenSSH サーバーをインストール
  ansible.builtin.package:
    name: openssh-server
    state: present
  become: yes
